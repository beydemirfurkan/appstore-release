# Secrets — never commit
*.p8
AuthKey_*.p8
credentials.json
*.p12
*.mobileprovision
dist.*
secrets/
.env

# A user's filled config carries their App Review contact details — keep the template only
config.json
appstore.config.json
.appstore-release/

# Node
node_modules/

# OS
.DS_Store

# Build / tooling
types/
*.tgz
