{
  "mcpServers": {
    "appstore-release": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/mcp/server.mjs"],
      "env": {
        "ASC_KEY_ID": "${ASC_KEY_ID}",
        "ASC_ISSUER_ID": "${ASC_ISSUER_ID}",
        "ASC_P8_PATH": "${ASC_P8_PATH}",
        "ASC_APP_ID": "${ASC_APP_ID}",
        "APPSTORE_CONFIG": "${APPSTORE_CONFIG}"
      }
    }
  }
}
